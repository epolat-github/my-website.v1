import Layout from "../components/layout";

const Blog = () => (
  <Layout>
    <article id="portfolio-container">
      <section>
        <h1>CONTACT</h1>
      </section>
    </article>
  </Layout>
);

export default Blog;
